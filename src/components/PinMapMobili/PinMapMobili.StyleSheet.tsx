import { StyleSheet } from 'react-native'

const PinMapMobiliStyleSheet = StyleSheet.create({
    pinMapMobiliView: {
        justifyContent: 'center',
        alignContent: 'center'
    },
    pinMapMobiliDescription: {
        marginLeft: -4
    },
});

export default PinMapMobiliStyleSheet;