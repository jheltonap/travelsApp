import { StyleSheet} from 'react-native'

const TravelsStyleSheet = StyleSheet.create({
    cardList: {
        marginHorizontal: 10,
    },
    cardTitle: {
        fontSize: 18,
        marginVertical: 18,
        marginLeft: 18
    }
});

export default TravelsStyleSheet;